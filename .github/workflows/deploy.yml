name: Deploy to GitHub Pages
on:
  push:
    branches: [main]
    paths:
      - 'apps/html-grundlagen/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: deploy-html-grundlagen
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Sync app to docs
        run: |
          set -e
          rm -rf docs/html-grundlagen
          mkdir -p docs/html-grundlagen
          cp -r apps/html-grundlagen/* docs/html-grundlagen/

      - name: Commit and push docs
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/html-grundlagen
          if git diff --staged --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Update docs/html-grundlagen"
          git push
